# Agent start prompt

Use this as the short launch instruction for the primary agent:

`Read AGENTS.md and CLAUDE.md, initialize the Beads sprint graph with scripts/bootstrap-beads.sh if needed, then use bd ready as the sole task authority and execute the P0 graph to completion using Superpowers and parallel subagents where useful; do not re-brainstorm or expand scope, optimize ruthlessly for a polished deterministic 60–90 second demo within one hour, run the final build/privacy/timed-E2E gate, commit cohesive changes, and push main.`

For specialist subagents, assign exactly one ready bead and tell them to read `docs/AGENT_ROLES.md` before editing.

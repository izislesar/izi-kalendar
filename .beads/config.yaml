# Beads repository configuration
#
# Intentionally no sync.remote here.
# This sprint uses a local Beads database; normal source code still uses GitHub
# through git's origin remote. A git-backed Dolt remote can be configured later
# with `bd dolt remote add` if persistence across machines is ever needed.
